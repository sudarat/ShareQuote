<h1>Listing posts</h1>

<table>
  <tr>
    <th>User</th>
    
    <th>Message</th>
    <th>Vote all</th>
    <th>Vote week</th>
    <th>Time share</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.user %></td>
    <td><%= post.message %></td>
    <td><%= post.vote_all %></td>
    <td><%= post.vote_week %></td>
    <td><%= post.time_share.strftime("%Y-%m-%d %H:%M:%S")  %></td>
    <td></td>
    <td><%= link_to 'Vote up', voteup_post_path(post) %> </td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
